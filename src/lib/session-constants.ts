/** Stored durationMins value for sub-minute (dev) timer presets. */
export const SUB_MINUTE_SESSION_MINS = 0;

export const HISTORY_LIMIT = 7;
export const MAX_SESSION_DURATION_MINS = 180;
export const MAX_JOURNAL_LENGTH = 2000;
export const BREATHING_CYCLE_COUNT = 3;

export const AI_FALLBACK_RESPONSE =
  "Your check-in is saved. The coach is briefly unavailable — take 3 cycles of 4-7-8 breathing (inhale 4s, hold 7s, exhale 8s) before your next block.";
